name: pypi

on: push

jobs:
  build-n-publish:
    name: build and publish
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: python
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

